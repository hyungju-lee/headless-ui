import { CSSProperties, ReactNode } from 'react';
interface HSingleDatePickerPrevButtonProps {
    children: ReactNode;
    className?: string;
    style?: CSSProperties;
}
declare const HSingleDatePickerPrevButton: ({ children, className, style }: HSingleDatePickerPrevButtonProps) => import("react/jsx-runtime").JSX.Element;
export default HSingleDatePickerPrevButton;
