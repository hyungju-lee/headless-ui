import { default as React, ReactNode } from 'react';
interface ToastPopupCloseButtonProps {
    children: ReactNode;
    className?: string;
    style?: React.CSSProperties;
}
declare const HToastPopupCloseButton: {
    ({ children, className, style }: ToastPopupCloseButtonProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
export default HToastPopupCloseButton;
