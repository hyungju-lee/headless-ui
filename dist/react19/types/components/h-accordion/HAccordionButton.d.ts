import { default as React } from 'react';
interface Props {
    children: React.ReactNode;
    className?: string;
    style?: React.CSSProperties;
}
declare function HAccordionButton({ children, className, style }: Props): import("react/jsx-runtime").JSX.Element;
export default HAccordionButton;
